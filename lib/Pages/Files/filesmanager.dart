import 'package:flutter/material.dart';

class FilesManager extends StatelessWidget {
  const FilesManager({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
